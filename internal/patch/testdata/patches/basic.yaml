defaults:
  conflict: patch_wins

patches:
  - cidr: 203.0.113.0/24
    op: merge
    set:
      custom.source: "manual_override"
      geo.country.iso_code: "DE"

  - cidr: 203.0.113.0/25
    op: delete_field
    field: traits.is_anonymous_proxy
